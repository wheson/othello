require './board.rb'

class ai
  def initialize()
  
  end
  
  def evaluate(board)

  end

  def sort(board, )

  def alphabeta(board, limit, alpha, beta)
    if board.is_game_end() == true \
    || limit == 0
      
    end
  end
end
